/**
 * @file bridge.h
 * @brief Focused bridge-state, move-generation, and DDS-leaf search include.
 *
 * The implementation still lives behind `alpha_mu_core.h`, but this wrapper
 * marks the intended bridge-search boundary separately from CLI/test plumbing.
 */

#ifndef DDS_TEST_ALPHA_MU_BRIDGE_H
#define DDS_TEST_ALPHA_MU_BRIDGE_H

#include "../alpha_mu_core.h"

#endif
